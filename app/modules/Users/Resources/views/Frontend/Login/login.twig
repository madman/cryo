{% extends "base.twig" %}

{% block content %}
    <form action="{{ path('users_secured') }}" method="post">
        <div class="error">{{ error }}</div>

        <label>{{ 'Email' }}: <input type="text" name="_username" value="{{ last_username }}"/></label>
        <label>{{ 'Пароль' }}: <input type="password" name="_password" value=""/></label>

        <input type="submit" value="{{ 'Войти' }}"/>
    </form>
{% endblock %}
